<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Imagem do Evento -->
        <ImageView
            android:id="@+id/imgEvento"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:src="@drawable/exemplo_evento" />

        <!-- Nome do Evento -->
        <TextView
            android:id="@+id/tvNomeEvento"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nome do Evento"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="16dp" />

        <!-- Data e Hora -->
        <TextView
            android:id="@+id/tvDataHora"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="01/07/2025 às 10:00"
            android:textSize="16sp"
            android:layout_marginTop="4dp" />

        <!-- Localização -->
        <TextView
            android:id="@+id/tvLocal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Rua da Tecnologia, 123"
            android:textSize="16sp"
            android:layout_marginTop="4dp" />

        <!-- Artistas/Palestrantes -->
        <TextView
            android:id="@+id/tvArtistas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Artistas: Fulano, Ciclano"
            android:textSize="16sp"
            android:layout_marginTop="8dp" />

        <!-- Botão de Compra -->
        <Button
            android:id="@+id/btnComprar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Comprar Ingresso"
            android:layout_marginTop="24dp"
            android:backgroundTint="@color/purple"
            android:textColor="@android:color/white" />

    </LinearLayout>
</ScrollView>
